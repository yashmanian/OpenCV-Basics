import cv2
import numpy as np
from matplotlib import pyplot as plt

img = cv2.imread("/home/yashmanian/images/ironman.jpg",0)
color = ['b','g','r']
for i,col in enumerate(color)
	hisrt = cv2.calcHist([img],[i],None,[256],[0,256])
	plt.plot(histr,
color = col)


cv2.waitKey(0)
cv2.destroyAllWindows()
